# 统一换行符：仓库内一律使用 LF，避免跨平台 diff 噪音
* text=auto eol=lf

# Windows 批处理脚本保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做转换
*.png binary
*.jpg binary
*.zip binary

# 语言统计：templates 下的 .java.tpl 归属 Java
# 否则 GitHub 会因 .tpl 后缀把仓库主语言识别成 Smarty，按 Java 筛选时搜不到
*.java.tpl linguist-language=Java
